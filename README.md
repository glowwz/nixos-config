# nixos-config

NixOS Personal Configuration

## Installation

```bash
git clone https://github.com/glowwz/nixos-config.git
cd nixos-config
sudo nixos-rebuild switch --flake .#nix
```

You can replace the hostname and username `glowz` and `nix` with your hostname and username, thats all :3

